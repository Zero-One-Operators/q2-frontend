'use client';

import { Box } from '@mui/material';

const SideBar = () => {
  return (
    <>
      <Box
        sx={{
          display: 'flex',
          height: '100%',
          width: '100%',
          marginTop: '10px',
        }}
      >
        <p> Will be sidebar </p>
      </Box>
    </>
  );
};

export default SideBar;
