"use client"

import React, { useState } from "react";
import { Box } from '@mui/material';

const SideBar = () => {


  return (
    <>
        <Box sx={{ display: 'flex',
                    height: '100%',
                    width: '100px',
                    backgroundColor: 'red',
        }}>
            <p> Will be sidebar </p>
      
        </Box>
    </>
  );
};

export default SideBar;
